#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../config/boot"
require_relative "../lib/runtime_configuration"

begin
  RuntimeConfiguration.validate!
  puts "Runtime configuration is valid"
rescue RuntimeConfiguration::Error => error
  warn error.message
  exit 1
end
